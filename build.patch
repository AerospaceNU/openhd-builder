diff --git a/OpenHD/build_rk3588_cross.sh b/OpenHD/build_rk3588_cross.sh
index 0d9ff525..903e72d1 100755
--- a/OpenHD/build_rk3588_cross.sh
+++ b/OpenHD/build_rk3588_cross.sh
@@ -14,8 +14,8 @@
 set -e  # Exit on error
 
 # Configuration
-TOOLCHAIN_ROOT="/opt/gcc-12.2.0"
-SYSROOT_PATH="/opt/rk3588_debian12_kernel6_10/sysroot"
+TOOLCHAIN_ROOT="/usr"
+SYSROOT_PATH="/"
 CROSS_PREFIX="aarch64-linux-gnu"
 BUILD_DIR="build_rk3588"
 INSTALL_PREFIX="/opt/openhd"
@@ -105,7 +105,7 @@ if [ ! -f "CMakeCache.txt" ] || [ "$RECONFIGURE" = true ] || [ "$CLEAN_BUILD" =
           -DENABLE_AIR=ON \
           -DENABLE_USB_CAMERAS=ON \
           -DCMAKE_VERBOSE_MAKEFILE=ON \
-          -G "Unix Makefiles" \
+	  -GNinja \
           ..
 
     if [ $? -ne 0 ]; then
@@ -120,7 +120,7 @@ fi
 print_status "Building OpenHD for RK3588 (incremental)..."
 
 # Build with parallel jobs - make will only rebuild changed files
-make -j$(nproc)
+ninja -j$(nproc)
 
 if [ $? -eq 0 ]; then
     print_status "Build completed successfully!"
diff --git a/OpenHD/cmake/rk3588-toolchain.cmake b/OpenHD/cmake/rk3588-toolchain.cmake
index 18b0e645..c3866380 100644
--- a/OpenHD/cmake/rk3588-toolchain.cmake
+++ b/OpenHD/cmake/rk3588-toolchain.cmake
@@ -11,8 +11,8 @@ set(CMAKE_SYSTEM_PROCESSOR aarch64)
 
 # ── Paths ────────────────────────────────────────────────────────────────────
 set(CROSS_COMPILE_PREFIX "aarch64-linux-gnu")
-set(TOOLCHAIN_ROOT "/opt/gcc-12.2.0")
-set(SYSROOT_PATH "/opt/rk3588_debian12_kernel6_10/sysroot")
+set(TOOLCHAIN_ROOT "/usr")
+set(SYSROOT_PATH "/")
 
 # ── Compilers (custom GCC 12.2.0, sysroot is built-in) ──────────────────────
 set(CMAKE_C_COMPILER   "${TOOLCHAIN_ROOT}/bin/${CROSS_COMPILE_PREFIX}-gcc")
@@ -53,7 +53,7 @@ find_program(PYTHON_EXECUTABLE NAMES python3 python
 set(Python3_EXECUTABLE "${PYTHON_EXECUTABLE}" CACHE FILEPATH "" FORCE)
 
 # Ensure host make is used
-find_program(CMAKE_MAKE_PROGRAM NAMES make gmake
+find_program(CMAKE_MAKE_PROGRAM NAMES ninja
     PATHS /usr/bin /usr/local/bin
     NO_CMAKE_FIND_ROOT_PATH
     REQUIRED)
diff --git a/install_build_dep.sh b/install_build_dep.sh
index ea0dffb4..47665803 100755
--- a/install_build_dep.sh
+++ b/install_build_dep.sh
@@ -27,9 +27,9 @@ set -e
 PLATFORM="$1"
 
 
-BASE_PACKAGES="libpoco-dev clang-format libusb-1.0-0-dev libpcap-dev libsodium-dev libnl-3-dev libnl-genl-3-dev libnl-route-3-dev libsdl2-dev"
-VIDEO_PACKAGES="libgstreamer-plugins-base1.0-dev libv4l-dev"
-BUILD_PACKAGES="git build-essential autotools-dev automake libtool python3-pip autoconf apt-transport-https ruby-dev cmake"
+BASE_PACKAGES="libpoco-dev:arm64 clang-format libusb-1.0-0-dev:arm64 libpcap-dev:arm64 libsodium-dev:arm64 libnl-3-dev:arm64 libnl-genl-3-dev:arm64 libnl-route-3-dev:arm64 libsdl2-dev:arm64"
+VIDEO_PACKAGES="libgstreamer-plugins-base1.0-dev:arm64 libv4l-dev:arm64"
+BUILD_PACKAGES="git autotools-dev automake libtool python3-pip autoconf apt-transport-https ruby-dev cmake"
 
 
 function install_pi_packages {
@@ -41,7 +41,7 @@ PLATFORM_PACKAGES="libunwind-dev gstreamer1.0-plugins-bad gstreamer1.0-plugins-u
 PLATFORM_PACKAGES_REMOVE=""
 }
 function install_rock_packages {
-PLATFORM_PACKAGES="libpoco-dev gstreamer1.0-plugins-bad gstreamer1.0-plugins-ugly"
+PLATFORM_PACKAGES="libpoco-dev:arm64 gstreamer1.0-plugins-bad:arm64 gstreamer1.0-plugins-ugly:arm64"
 PLATFORM_PACKAGES_REMOVE=""
 }
 
